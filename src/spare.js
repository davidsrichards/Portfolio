/* import { useSelector } from "react-redux";

function Button() {
  const { height } = useSelector((state) => state.slice);
  return (
    <button
      className={`bg-white w-[8rem] h-[2.9rem] rounded-md absolute  right-12 text-black ring-${
        height ? "1" : "0"
      } ring-black ring-1 lg:block hidden`}
    >
      LOGIN
    </button>
  );
}

export default Button; */
